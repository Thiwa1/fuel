<UserControl x:Class="WpfApp1.Views.PaymentUploadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Select Schedule:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
            <ComboBox x:Name="cmbSchedules" Width="200" DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,20,0"/>

            <TextBlock Text="Payment Date:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
            <DatePicker x:Name="dpPaymentDate" Width="120" Margin="0,0,20,0"/>

            <Button Content="Upload Excel" Click="btnUpload_Click" Padding="10,5" Background="#2196F3" Foreground="White" FontWeight="Bold"/>
        </StackPanel>

        <DataGrid x:Name="dgPreview" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" AlternatingRowBackground="#F0F0F0" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Emp No" Binding="{Binding EmployeeNumber}" Width="80"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Account Number" Binding="{Binding AccountNumber}" Width="120"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:N2}}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="300">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Red"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="Ready">
                                    <Setter Property="Foreground" Value="Green"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock Text="Total Amount:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock x:Name="txtTotalAmount" Text="0.00" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button x:Name="btnSave" Content="Save to Database" Click="btnSave_Click" Padding="15,8" Background="#4CAF50" Foreground="White" FontWeight="Bold" IsEnabled="False" Margin="20,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
