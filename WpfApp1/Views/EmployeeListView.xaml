<UserControl x:Class="WpfApp1.Views.EmployeeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
            <TextBox x:Name="txtSearch" Width="200" Padding="5" TextChanged="txtSearch_TextChanged"/>
            <Button Content="Add New Employee" Margin="20,0,0,0" Padding="10,5" Click="btnAddEmployee_Click" Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
            <Button Content="Refresh" Margin="10,0,0,0" Padding="10,5" Click="btnRefresh_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgEmployees" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" AlternatingRowBackground="#F0F0F0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                <DataGridTextColumn Header="Emp No" Binding="{Binding EmployeeNumber}" Width="80"/>
                <DataGridTextColumn Header="Calling Name" Binding="{Binding CallingName}" Width="150"/>
                <DataGridTextColumn Header="Account Name" Binding="{Binding AccountName}" Width="150"/>
                <DataGridTextColumn Header="Bank" Binding="{Binding Bank}" Width="100"/>
                <DataGridTextColumn Header="Branch" Binding="{Binding Branch}" Width="80"/>
                <DataGridTextColumn Header="NIC" Binding="{Binding NicNo}" Width="100"/>
                <DataGridTextColumn Header="Account Number" Binding="{Binding AccountNumber}" Width="120"/>
                <DataGridTextColumn Header="Area" Binding="{Binding Area}" Width="*"/>
                <DataGridTemplateColumn Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="View History" Click="btnHistory_Click" Padding="5,2" Background="#007B5E" Foreground="White" BorderThickness="0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
