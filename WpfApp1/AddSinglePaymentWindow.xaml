<Window x:Class="WpfApp1.AddSinglePaymentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Add Single Payment" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Select Employee:" Margin="5"/>
        <ComboBox Grid.Row="0" Grid.Column="1" x:Name="cmbEmployees" DisplayMemberPath="CallingName" SelectedValuePath="Id" Margin="5" IsEditable="True"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Amount:" Margin="5"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtAmount" Margin="5"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Date:" Margin="5"/>
        <DatePicker Grid.Row="2" Grid.Column="1" x:Name="dpDate" Margin="5"/>

        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnSave" Content="Save" Width="80" Margin="5" Click="btnSave_Click"/>
            <Button x:Name="btnCancel" Content="Cancel" Width="80" Margin="5" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
